/**
 * Requêtes DB pour les formations, modules et leçons.
 * Toutes les requêtes filtrent par tenant_id — la RLS Supabase le garantit
 * au niveau DB, mais on filtre aussi explicitement en application.
 */

export {}; // placeholder — sera implémenté lors de l'intégration Supabase
